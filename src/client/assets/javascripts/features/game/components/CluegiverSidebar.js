/**
 * Created by chris on 1/12/17.
 */



const giveClueAction = (word, count) => {
  const action = this.validateAction(thisPerson, gameplay, ACTION_TYPES.GIVE_CLUE);
  if (action) {
    action(userId, word, count);
  }
};
